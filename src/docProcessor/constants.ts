import path from 'path'

/** This defines the path to the doc config file in the repo */
export const CONFIG_FILE_NAME = path.join('doc', 'docConfig.json')

/** The directory where the images are located */
export const CONFIG_IMAGES_DIR = path.join('doc', 'images')

/** The prefix for all the images created by this process */
export const IMAGE_NAME_PREFIX = 'CIL_CONVERTER_'
